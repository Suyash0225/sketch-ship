GOOGLE_API_KEY=your-google-ai-studio-key-here

# SerpApi key (https://serpapi.com, free-trial credit works) -- used for real
# reverse-image "web-scan" leak detection via the Google Lens engine.
# Leave unset to skip real web scanning (falls back to no-op).
SERPAPI_KEY=your-serpapi-key-here

# Public URL this backend is reachable at, so SerpApi's servers can fetch
# uploaded asset images (it needs a URL, not raw bytes). Point this at an
# ngrok tunnel (`ngrok http 8000`) or your real public deployment URL.
PUBLIC_BASE_URL=http://localhost:8000
